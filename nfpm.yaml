name: "${REPO_NAME}"
arch: amd64
platform: linux
version: "${APP_VERSION}"
description: "${APP_NAME} desktop application"
maintainer: "Your Name <you@example.com>"   # TODO
homepage: "https://github.com/${GITHUB_REPOSITORY}"

contents:
  # Install the full onedir bundle to /opt
  - src: dist/${REPO_NAME}/
    dst: /opt/${REPO_NAME}/

  # Symlink main binary onto PATH
  - type: symlink
    src: /opt/${REPO_NAME}/${REPO_NAME}
    dst: /usr/bin/${REPO_NAME}

  - src: packaging/${REPO_NAME}.desktop
    dst: /usr/share/applications/${REPO_NAME}.desktop
